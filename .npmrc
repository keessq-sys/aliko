# Cloudflare Pages exposes NODE_ENV=production during its build. The Svelte,
# Vite and adapter packages are build-time devDependencies, while workerd uses
# an OS-specific optional package. Explicit includes keep both groups available.
include[]=dev
include[]=optional
